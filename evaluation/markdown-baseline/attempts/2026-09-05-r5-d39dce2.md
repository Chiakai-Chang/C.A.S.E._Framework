# r5 method attempt incident — `d39dce2`

Status: controller/terminal reconstruction; not a runner-atomically-persisted outcome record

## Known execution order

1. The authorized launcher started the exact configured `llama-server.exe`; endpoint inventory reported the configured Qwen GGUF and context `262144`.
2. The first runner invocation stopped at its repository `git rev-parse HEAD` preflight because Git rejected the worktree as dubious ownership. No model request occurred in this invocation and no result file was created. The controller did not modify global Git configuration.
3. The second invocation supplied only the exact worktree as process-local `safe.directory`. It hashed the model/server artifacts and executed B0 `EVAL-M0-001`, including model requests. At persistence, Node returned:

   `EISDIR: illegal operation on a directory, link '<redacted-results-temp>' -> '<redacted-r5-record>'`

4. The runner emitted a stable terminal persistence failure for `20260905-qwen-b0-eval-m0-001-r5`. No target record or temporary file remained, so the actor transcript and outcome are unavailable and receive no evidentiary credit.
5. The controller interrupted the runner immediately after that terminal failure. B0 cases 002–004 and all M0 cases were not run in this attempt.
6. The exact server process was verified by PID and executable path, then stopped before method repair.

The terminal did not capture wall-clock instants for either invocation, so this artifact does not invent them. Git commit order preserves that the incident was recorded after method commit `d39dce25393d83fdf1829b344920274d0ff86e83` and before any storage amendment or replacement run.

## Eligibility

This attempt is invalid and excluded. A later method revision must use new record IDs; it must not recreate or overwrite the missing r5 record.
