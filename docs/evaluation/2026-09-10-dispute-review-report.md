# 已修復成果的核對通過；異議分支尚未驗證

2026-09-10。產品基準提交 `c82cb39` 加上尚未提交的異議實作；實際程式雜湊保存於[證據](2026-09-10-dispute-review-evidence.json)的 `codeHashes`。這是先前已完成測試的整理，沒有重新呼叫模型。

## 結果與成本

卷宗 `completed`、成果評分通過、來源未變、沒有額外產物；執行期間 `codeUnchanged=true`。只經 reviewer → integrator，**沒有 planner、異議申請或 worker 返工**。

| 角色 | 軌跡耗時 | SDK 累計 tokens（含 cacheRead） |
|---|---:|---:|
| reviewer | 101.983 秒 | 85,449 |
| integrator | 106.209 秒 | 38,003 |

本次牆鐘時間 220.292 秒，兩個 session 的 SDK 累計為 123,452 tokens；含角色外開銷，不以角色耗時相加取代總時間。兩者 `traceComplete=true`、`statsError=null`。累計 tokens 不是峰值 context；實際峰值未知，宣告 context 為 32,768。沿用非思考模式、單次輸出 4,096、最多 16 turns、額外診斷 600 秒。模型識別及服務設定保留於證據，未更改使用者設定。

本次匯入前次修好的真實產物，透過正式操作建立 submitted 起點，不是原 worker-only session 的自然接續。前次 worker 約 210.787 秒、off 修復約 131.896 秒及其他失敗成本分別保留；不得拼成一次原預算內完成。SDK 全相依樹及 GGUF 未逐位元凍結，沒有提供任務 oracle 給模型。

## 對目標有什麼幫助？

這次確認已修好成果能走過目前版本的一般獨立核對與整合；不能證明異議功能有效，更不能證明 CASE 相對一般 pi 更省成本或品質更高。9/9 的錯誤否決仍是有效反例，沒有被本次成功抵銷。

異議功能的目的，是讓模型針對錯誤指控查證，保留正確成果，減少使用者救援；並非把每份成果都多審一次。下一個有鑑別力的驗證是已規劃的受控錯誤否決診斷，通過後才進行正常完整旅程。若不能改善這個已觀察到的問題，就應調整方法，而不是以更多角色或文件補足表面完整度。

## 工程與環境：獨立結論

整理時重跑針對性測試：49 通過、1 跳過；完整 kit：278 通過、1 跳過、0 失敗。跳過項是 Windows 不允許建立符號連結的反證來源測試，不宣稱該環境已實測此路徑。

使用既有 Node 附帶 npm、專案內快取及 `--ignore-scripts` 的封裝預覽成功：35 個檔案，包含 `core/review-dispute.mjs`、pi runner、SDK 與技能參照，未包含本機快取。這只是當時內容的 dry run，不是已發布套件；後續文件更新後須重查最終封裝。

自動審查用量錯誤發生在測試之後的停止模型操作，沒有讓本次結果失效；該被拒操作未重送、未繞過。沒有因此重建 pi／npm、清理 AppData 或使用額度重置。獨立收尾工作已繼續，不以此環境事件新增 CASE 產品需求。

## 保存與可追溯性

本機原始檔 `.npm-cache/dispute-review-2026-09-10.json` SHA-256：`1d0f230798b5df1d505aa70b8f8504ae46194442d0bbce1e6d233834cefc9153`。

公開證據 SHA-256：`4e32a0bd062dc7f91f7f7cf1add75a824ccb4c806de3a954388ff865f9c0d051`。使用檔案編輯工具保存時只增加檔尾一個 LF；已比較全文及解析後 JSON 相等，沒有修改結果欄位。原始檔保留，不宣稱兩檔逐位元相同。公開資料仍含本機路徑與 session 識別碼；敏感字串檢查未發現憑證候選，不代表一般 CASE 紀錄會自動遮蔽私人資料。

下一步依[接續計畫](../superpowers/plans/2026-09-10-recovery-and-delivery.md)。尚未合併、推送或完成異議分支的真實模型驗收。

## 下一次受控診斷：事前設定

使用者同意接續後，新增 evaluation-only `dispute-off` 選項。沿用相同已修好成果與原契約，真實 reviewer 核對後，第一次 integrator 回覆改為逐字重播 9/9 保存的錯誤否決；明確記為 synthetic，不算本次模型生成。接下來的 planner 與 integrator 使用真實本機模型，不附正確答案、預期引用或強制通過要求。產品程式與提示不為測試修改。

設定沿用 off、32,768 context、4,096 輸出、16 turns 與一次額外 600 秒上限。程式與歷史否決來源在 prepare 時凍結，run 時核對；不在同次測試中修改。新增三項測試檢查首次否決只重播一次、重核確實呼叫後續執行介面、回覆標示及驗證失敗不暗中改跑模型。

通過條件同時要求：真正形成 accepted 異議、完整卷宗完成、成果評分與來源完整性通過、成果雜湊不變、沒有 worker 返工、程式未變。若模型選擇返工或錯誤受阻，即使其他部分成功也不算本診斷通過。只執行一次；新結果另存，不改寫本頁前次約 220 秒的證據。
