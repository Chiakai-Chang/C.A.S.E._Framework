# 目標閉環：覆蓋復盤與實作次序

日期：2026-09-11。承接使用者要求：不要只驗過程、反覆測題或無限研究；在既有授權內，經修復與跨 session 接續完成成果。

## 修正判斷

目標仍見 [GOALS](../GOALS.md) 及[原案分層對照](../research/2026-09-05-layered-cooperation-direction.md)。實際 runner 已有核對返修、全域失敗重規劃、持久新增工作及相依處置，不能再說沒有閉環。最近模型案例是所有核對都誤判，未啟動返修；中斷／預算則是另一類缺口。

[官方實務](../research/2026-09-11-goal-loop-practices.md)支持保存未過驗收、實際檢查及安全重播；不需要導入新 runtime 或固定增加角色。

## 按責任邊界檢查覆蓋

下表避免只看成功路徑，不宣稱窮盡所有任務。跨欄問題依主要責任處理，不複製同義待辦。

| 面向 | 必須考慮 | 現況與下一步 |
|---|---|---|
| 目標與完成條件 | 漏需求、矛盾／歧義、目標變更 | 已有契約與驗收對應；不可把測題答案變成通用要求，重大目標變更仍由使用者決定 |
| 材料與 context | 缺料、過期、索引未讀、交接遺漏 | 已有版本與按需材料；來源有效不等於理解正確，接續仍需保留未解問題 |
| 執行與副作用 | 局部錯誤、部分寫入、工具中斷、舊程序仍在跑 | 已有自查及限定寫入；未知副作用不能盲目重播 |
| 驗收與返修 | 真失敗、誤放行、錯誤否決、修復回歸 | 已有檢查、返修及異議；接上適用的實際檢查，無機械檢查時仍須語意核對，不宣稱通用正確性保證 |
| 規劃與新增工作 | 缺前置、跨包修改、去重、獨立工作可繼續 | 已有 discovery／amendment；用完整旅程驗接線。固定重規劃上限是否提前停止仍需具體判斷 |
| 接續與資源 | session 結束、重啟、取消、期限、總預算、無進展 | 本批修正已過期仍開模型；尚未統一所有自動恢復分類，不能宣稱無人介入接續完成 |
| 結案與重開 | 包通過但整體有缺口、結案後發現錯誤、外部修改 | 全域驗收已存在；completed 目前直接返回，重開用 revise 並保守重驗。需評估同目標缺陷局部重開，不偷換授權 |

安全與成本橫跨各面向。跨工具與易用性是交付面：pi 自動化與可攜技能能力分開說明，README 必須交代開始方式、接續及限制。

## 本批實作與驗證

- runner 開始時取累計執行剩餘時間、核心首次 worker 開始後期限的較小值，每次模型呼叫前再檢查。保留原限制、不改預算定義、不重設額度。
- 先重現過期 submitted 卷宗仍呼叫 reviewer，修正後零模型呼叫、原成果與狀態保留；另涵蓋檢查期間期限到達。
- 真實命令閉環：錯誤加總被拒收 → 同 session 修復 → 整合斷線 → 重載磁碟狀態 → 新 session 只做整合 → 正確成果完成。失敗檢查與兩次 run 保留，沒有重做已核對寫入。
- `npm test --prefix workflow-kit`：338 項，336 通過、2 項平台權限跳過、0 失敗。模型回覆是受控替身，檔案、狀態、檢查命令及接續是真實執行；不是本機模型效果證明。本批沒有本機模型呼叫。

## 接下來依序交付

2026-09-12 收斂：下述安全恢復及既有檢查返修接線已完成工程驗證；正常本機兩個相依工作包亦完整通過，見[交付證據](../evaluation/2026-09-12-push-acceptance-report.md)。該正常任務未觸發新增工作／中斷，不能代替這些機制的實測。以此交付預覽批次；未覆蓋的串流中斷、自動恢復分類與語意誤驗保留限制，不再把後續研究全部當成 Git 推送前置條件，也不宣稱整體效益已證明。

### 已接上：唯讀核對的暫時性連線恢復

同卷宗最多自動恢復一次，記入 `run.transportRecoveries`，跨 run 累計、不增總 session／時間額度。SDK 在失敗時停止生成及壓縮、釋放 session，並提供程式產生的 `recovery:{version:1,safeToRetry}`。僅 reviewer／integrator、沒有可執行檢查、沒有已接受結果或進行中工具、停止及清理成功時才可為 true；不是模型自報安全。

runner 只接受明確連線錯誤 `ECONNRESET`／`ETIMEDOUT`／`ECONNREFUSED`／`EAI_AGAIN`，保存失敗與成本後，原角色換新 session 繼續。取消、預算耗盡、異議重核、未知錯誤、驗收不合格、worker 中斷與檢查命令不自動重播。自訂 transport 沒有此證據時維持停止；若自行提供則負責該信任邊界。

獨立審閱發現 pi 0.84.2 常將 provider 錯誤轉成 `stopReason:error` 正常返回，舊流程會誤走 JSON 格式修正。已修正為立即保留 `MODEL_PROVIDER_ERROR`，不再額外問一次。對現用 `openai-completions`，透過該 session 的公開 `agent.streamFunction` 與 `options.fetch` 保留 fetch rejection 的結構化 code／cause.code，避免被 provider 正規化丟失。收到 HTTP response 即清除此證據；不解析 errorMessage，不改全域 fetch、模型或服務設定。401／429 等 HTTP 錯誤字串不會觸發恢復。收到 headers 後才發生的串流中斷不在此捕捉範圍，仍誠實停止，不宣稱能恢復所有連線中斷。

使用本機實際 pi SDK 0.84.2、獨立暫存目錄及替代 HTTP fetch 做接線探測，原生 prompt 正常返回的 provider error 正確還原為 `ECONNRESET`，`safeToRetry:true`，1 次替代傳輸、0 次格式補問、0 次模型呼叫。替代 fetch 只存在於該測試 Node 程序，不改使用者設定。這是傳輸機制驗證，不是模型完成任務的證據。

新增測試覆蓋 SDK 安全判定、停止／清理失敗、重試失敗及重開不補額度；整段 SDK→runner 測試使用真實檔案工具與回條，確認失敗的整合 session 已釋放、新 session 接續完成且 worker 只寫入一次。模型生成使用受控替身，不是本機模型語意驗收證據。

此後續批次完整回歸：353 項，351 通過、2 項平台權限跳過、0 失敗。獨立審閱原先提出的原生錯誤接線問題已修正，重查未發現阻擋問題；headers 後串流中斷仍列限制。

1. **接續閉環**：區分唯讀失敗可重試、寫入中斷需確認、缺外部輸入需等待、預算耗盡需停。沿同卷宗保存成本；排除暫停時間若要採用，需調整契約與相容性，不能只刪核心檢查。
2. **成果核對**：讓已授權檢查的失敗具體回到修復交辦；語意核對要求與實際成果、支持／矛盾來源。不要硬編碼最近案例答案；必要的新介面再具體設計。
3. **完整旅程及入口**：先受控故障驗接線，再用有實際用途的本機任務驗新增工作、返修及接續後的最後成果。限定原總預算、保存失敗，不拼接局部成功；同步 README／V2／英文使用說明。

停止無假設抽樣、固定角色加碼及為單題擴張架構。前題雜湊異常保留待查，但不優先於完整工作閉環；此優先次序取代 holdout 報告的「先追雜湊」。尚未修好的語意誤驗，不因本批工程通過而撤銷。
