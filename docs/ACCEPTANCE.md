# 交付驗收

## v2 本輪實作驗收（2026-09-05）

版本 `2.0.0-preview.1`，工作分支 `feat/case-context-workflow`。本輪已實作全域契約 → 工作包 → pi 新 session 執行／核對 → 整合，並保留 v1。**流程功能與下列本地案例已驗證；完整效益驗收尚未完成，不將預覽版稱作普遍改善模型工作的已證實方案。**

| 範圍 | 實際證據 |
|---|---|
| 狀態、相依、版本、接續、限制與失敗 | 完整 Kit 回歸 61/61；包含禁止模型改操作、禁止對真實驗收失敗重問、失敗工具觀察／用量保留 |
| 原生 pi 安裝／執行／移除 | 實際 pi 0.84.2 loader、create/run、本地模型四 session、逐位元產物檢查；移除後卷宗及成果雜湊不變 |
| 真實程式修正 | CSV 五組開發配對全部保存，包含一次流程失敗；不是固定版本統計 |
| 不同任務與接續 | 資料整合、缺必要資料安全停止、保留 verified 上游接續三案各一次通過 |
| 套件範圍 | npm pack dry-run 29 檔，包含原生入口與共用核心，未包含 evaluation、私有快取或認證資料 |
| 文件 | 正體中文主入口、英文、技能、v1/v2 分流、架構與修改關聯同步；本地連結核對 |

全部模型結果及限制見[本地驗證報告](evaluation/case-v2-local-report.md)。新跨平台 CI 已改為跑完整 Kit，但本輪尚未推送，不能借用舊六組 CI 結果。測試過程也保留了啟動目錄相依、原生 SDK 載入失敗等修正紀錄。

尚缺：[設計第 11 節](design/2026-09-05-case-solution-design.md#11-驗收與效益判斷) 的五類任務、三種策略、每組三次完整對照，以及更長 context 的效益判斷。現有小任務結果反而支持直接完成更省成本；尚無理由全面強制拆分。一般 extension 預設只有檔案工具，沒有任意 shell／自動執行測試清單，不能把閱讀核對當程式測試。公開授權與 npm 發布也未決定。

因此本輪先保存可檢查的實作與證據於工作分支，不冒稱整個專案的效果驗收已完成或已推送。沒有把實測工作交回使用者，也沒有清除全域 pi、改 M0 或建立排程。

## v1 歷史驗收

範圍修正（2026-09-05）：本頁保留當時 Workflow Kit 的工具與文件驗收，不是原始 CASE 完整流程的驗收。重新確認的目標及尚缺能力見 [GOALS](GOALS.md) 與[重構決策](design/2026-09-05-constitution-context-reconstruction.md)；下列歷史結果不改寫為新目標已通過。

日期：2026-09-05。依使用者授權，以 [目標](GOALS.md) 驗收並準備推送遠端。

結論：**目前明訂的可攜 Workflow Kit 功能與文件交付通過；模型效益不在本次證據所能證明的範圍內。** 沒有把效益驗證轉交使用者作為交付前置條件，也不把工具測試當成品質提升證明。

| 目標 | 本次核對與結論 |
|---|---|
| 保留目標、約束與下一步 | CLI 與接續測試通過；精簡 context 截斷時提示補讀完整狀態 |
| 跨 session／宿主接續 | 獨立程序從兩種安裝位置操作同一任務通過；不冒稱跨產品模型 session 已測 |
| 品質與驗收 | 未齊備驗收不能 finish；reopen 重置舊驗收，要求重新確認；證據內容仍需實際核對 |
| 分工與使用者負擔 | 工作包、單一協調者、按需讀取及短任務免建檔的規則齊全；不強制角色、表單或排程 |
| 安裝與保存使用者資料 | 安裝、更新、備份移除、修改／外來內容拒絕與任務保留測試通過 |
| 文件整體性 | 獨立核對中英文指南、架構、範本、維護關聯及實作；76 個本地連結皆存在 |

本輪 Windows 操作測試 13/13 通過，技能結構驗證通過，diff 空白檢查通過。審閱發現 macOS 系統暫存路徑可能含符號連結，已將測試的暫存根先解析為實際路徑，沒有放寬產品路徑防護；跨平台 CI 的結果另行觀察。其餘修正為正體中文漏字。

程式與文件審閱未發現目前範圍內的發布阻擋問題。這不等於宣稱所有模型均能遵循、品質必然提升、token 成本必然下降或所有宿主均已完整實測。已知範圍仍以 [READINESS](../workflow-kit/docs/READINESS.md) 為準。

Git 遠端核對為使用者指定 repository，推送前尚無遠端分支，使用一般 main 推送而非 force。公開授權尚未選定；此次推送不包含替擁有者選授權、發布 npm 或清除全域 pi 等環境。

推送後補驗：首次 CI 五組通過，Windows Node 20 在測試開始前因不展開 glob 而失敗。已修正 CI 與所有目前使用入口為明列測試檔，未縮減案例或提高最低 Node 版本；保留首次失敗紀錄並重跑六組驗證。

最終結果：修正提交 `2a81bc8` 的 [六組 CI 全數通過](https://github.com/Chiakai-Chang/C.A.S.E._Framework/actions/runs/33968299883)，已推送遠端。功能、文件與工具跨平台交付驗收通過；模型效果與公開授權限制維持不變。
