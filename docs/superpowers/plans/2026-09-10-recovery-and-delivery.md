# 中斷後收斂與交付計畫

2026-09-11 新增反例：不同正常任務在 343.108 秒標完成，但一個語意欄位錯誤通過兩層核對；7 個引用本身有效。整案失敗、220,286 tokens 及四次假 ID 拒收均保留。下一步改為追查前題完整雜湊為何進入本次捏造 ID，以及主張／來源的語意核實；不先加角色、放寬驗收或重建環境。[結果與調查邊界](../../evaluation/2026-09-11-holdout-journey-report.md)。

2026-09-11 接續：使用者已同意回條 ID 引用介面，現已接入正常 pi 核對／整合，工程 333 通過、2 權限跳過；單獨 integrator 新案例正確一負一正，42.792 秒／10,066 tokens。[設計取捨、證據與成本復盤](../../evaluation/2026-09-11-receipt-review-report.md)。下一個有意義的驗證是不同正常任務的完整生命週期，不再重抽原主題；語意判斷、異議真實路徑與成本仍不可冒充完成。

> 執行者：使用 `superpowers:executing-plans` 逐項接續；本計畫不要求新增分工或重跑已完成研究。

**目標：** 讓本地模型在有限 context 下完成正確成果、合理修復與接續，減少使用者救援；本輪收斂既有異議功能、補足有鑑別力的驗證並整理交付。

**架構：** 保留共用核心、可攜技能與 pi 薄整合層。模型提出判斷，程式保障來源版本、權限、狀態及預算；不因 Codex 自動審查中斷改造 CASE 的產品架構。

**技術：** 既有 Node.js／ES modules、Node test runner、pi SDK 與使用者現用本機模型。

**依據：** [目標](../../GOALS.md)、[已同意的異議設計與中斷復盤](../../design/2026-09-10-review-dispute-boundary.md)、[原始構想對照](../../research/2026-09-05-layered-cooperation-direction.md)。本計畫是接續順序，不取代原需求追蹤。

## 全域限制與這次教訓

- 已成功的測試不為了補紀錄而重跑；原始資料先保存，報告可稍後整理。
- 保存證據、文件、封裝與停止模型各自執行；需權限的清理不作其他收尾的前置。
- 自動審查錯誤、模型行為、產品錯誤、npm 環境問題分開記錄。未知原因不推成全域環境損毀。
- 被拒絕的操作不改用其他路徑繞過；不重建 pi／npm、不清理 AppData、不兌換額度、不改模型或帳號設定。
- 模型服務先核對現況及來源；不重複啟動、不停止無法確認歸屬的程序。若需新權限，只暫停該操作並明確請求，不擋獨立工作。
- 沿用原總預算與權限，不以重試重置成本。不同批次、合成狀態與真實整案結果不得拼成一次端到端成功。
- 主要文件用臺灣正體中文；維護關聯遵循 `docs/MAINTENANCE.md`。本輪不加角色、排程、資料庫或通用 runtime。

## 1. 固定目前成果與接續位置

**檔案：** `.npm-cache/dispute-review-2026-09-10.json` → 新增 `docs/evaluation/2026-09-10-dispute-review-evidence.json` 及對應 `2026-09-10-dispute-review-report.md`；核對 `.gitattributes` 的證據換行規則。

**輸入／輸出：** 既有完成結果與來源雜湊 → 可追溯的公開證據及限制，不新增模型呼叫。

- [x] 核對原始結果、來源完整性、角色、模型設定、耗時與可取得的 usage；未取得成本標未知。檢查敏感資訊；若必須遮蔽，保留本機原始檔並在公開版列出遮蔽範圍，不宣稱公開版與原始檔逐位元相同。
- [x] 保存後核對內容／雜湊。報告明示約 220 秒、reviewer／integrator 通過，未觸發異議，且是匯入已修好成果的診斷，不是完整任務從頭完成。
- [x] 在異議設計勾選確已完成的實作項；未完成的中英文同步及模型分支驗證維持未完成。

**完成條件：** 即使模型關閉或對話中止，接手者也能找到結果、未完成項與下一個動作；無需重跑成功案例。

## 2. 收斂工程行為，不擴張功能

**檔案：** `workflow-kit/skills/case-workflow/scripts/core/review-dispute.mjs`、`store.mjs`、`workflow-kit/integrations/pi/{runner,sdk-session}.mjs`；測試 `workflow-kit/tests/{review-dispute,feedback,pi-sdk,pi-discovery-sdk}.test.mjs`。

**輸入／輸出：** 現有未提交改動及已回覆的審閱問題 → 可交付的固定版本；介面沿用 `reviewSnapshot(caseId)`、`validateReviewDispute(caseId,snapshot,dispute)` 及互斥 `reviewDispute` 回覆。

- [x] 核對現有差異及先前審閱指出的中斷恢復修正；不重做相同審閱。重點為保留成果、假引用拒收、真缺陷不可繞過、原版本一次重核、明確修訂後恢復及成本累計。
- [x] 執行 `node --test workflow-kit/tests/review-dispute.test.mjs workflow-kit/tests/feedback.test.mjs`，再執行 `npm test --prefix workflow-kit`；保留失敗及 Windows 權限跳過原因。若發現新錯誤，先加可重現失敗測試，再修正並重驗受影響範圍。
- [x] 執行 `git diff --check`。分清工程測試結果與模型效果；固定待測程式雜湊，模型測試期間不邊改邊測。

**完成條件：** 沒有未處理的阻斷問題，工程回歸有新鮮結果；不以測試數代替產品效益。

## 3. 只補真正缺少的模型證據

**檔案：** 參照 `workflow-kit/evaluation/review-repair-replay.mjs` 與 `workflow-kit/tests/feedback.test.mjs` 的既有接線；新診斷如需程式，獨立放在 `workflow-kit/evaluation/`，不可把測試答案注入產品提示或改寫歷史證據。所有結果存入 `docs/evaluation/`，報告連到固定程式版本。

**輸入／輸出：** 既有正確成果、9/9 真實錯誤否決、當前契約及現用模型 → 異議機制的受控診斷，以及獨立的正常旅程結果。

- [x] 先寫下診斷設定與評分條件，再執行一次受控異議診斷：以保存的錯誤否決作明確標記的外部測試輸入，讓真實 planner 自行找來源提出反證，再由新 integrator session 核對。不得提供期望引用或要求「必須通過」。
- [x] 驗收 planner 是否真的提出有效引用、integrator 是否完整核對、正確成果雜湊是否不變，以及是否沒有 worker 返工。沒提出異議、引用無效或重核未解都算診斷未通過，不能改算一般成功。（已執行並裁定未通過，並非勾選即通過。）
- [ ] 診斷通過後，執行一次未注入否決的正常完整旅程，由規劃到產物、核對及整合；用既有任務與來源評分，保留所有角色與成本。這是完整旅程驗證，不是普遍效益統計。
- [ ] 每次沿用既有 600 秒上限及原角色／session 預算；額外診斷成本明列，不混入原案預算。受控診斷不通過就先定位第一個失敗邊界，不啟動正常旅程、不原樣抽樣到成功；有具體修正及工程反例後才安排新版本驗證。

**完成條件：** 能分別回答「工程機制能用」「本機模型能使用該機制」「正常整案能完成」。任一未過如實保留，不拿其他通過項抵銷，也不要求使用者代替實測。

## 4. 同步採用文件與完成交付

**檔案：** 根 `README.md`、`docs/STATUS.md`、`MAP.md`；`workflow-kit/README.md`、`docs/{READINESS,GUIDE.en,V2,ARCHITECTURE}.md`、`skills/case-workflow/references/v2-contracts.md`、`package.json`；按實際影響核對技能及範本，不機械重寫全部文件。

**輸入／輸出：** 固定實作與各類證據 → 人類可讀的用途、操作、支援範圍及真實交付狀態。

- [ ] README 先回答做什麼、什麼情境值得使用、怎麼開始、失敗如何接續、證據與限制。新手不必先讀歷史；pi 原生自動化與其他工具的可攜技能明確區分。
- [ ] READINESS 集中列實測；STATUS 分開本機未提交、已提交、已合併及遠端狀態。把舊的「尚未實作」改為有日期的最新狀態，保留歷史結果。英文與命令同步；連結逐一核對。
- [ ] 在 `workflow-kit` 工作目錄，使用現有 Node 附帶 npm 檢查封裝：`node "C:/Program Files/nodejs/node_modules/npm/bin/npm-cli.js" pack --dry-run --json --cache "D:/MyProject/C.A.S.E._Framework/.npm-cache" --ignore-scripts`。這是處理 npm 相依／快取問題的既有工具檢查，不是替代被拒絕的停止模型操作。失敗獨立記錄，不改全域安裝。
- [ ] 核對封裝包含新核心模組與技能必要參照、不含本機證據快取。對最終實際程式變更跑完整 kit 測試；只有文件變更則核對內容與連結。
- [ ] 依既有交付授權與當下安全限制完成提交、整合及推送；先核對分支及遠端變動，不覆寫使用者修改。需要權限則明確處理該一步，不能把本機完成寫成已推送。

**完成條件：** 使用者知道可用到哪裡、如何用及限制；新功能的實作、證據、文件與實際 Git 狀態一致。若模型核心驗收未通過，仍整理並保存工程成果，但不得標示整體目標完成。

## 決策與接續

**9/11 執行交辦收斂：** 26 次來源回傳核實無誤，角色混淆早於壓縮；直接執行交辦診斷 169.286 秒正確產出後，已實作資訊完整的 pi 呈現接線。正常完整流程從空白成果 **547.780 秒通過四角色驗收**，成果、來源、範圍與 trace 檢查通過，人工介入 0。工程 323 通過／2 權限跳過；本批診斷與正常任務合計 575,000 SDK 累計 tokens，另保留零 tokens 啟動失敗。不要再以沒有可完成案例為前提；下一步聚焦引用可信度、成本與不同任務，未證明普遍可靠，不合併／推送為全部交付完成。見[來源使用與完整驗收](../../evaluation/2026-09-11-worker-grounding-report.md)。以下保留歷史決策。

**9/11 初始規劃接續：** 已分開初始／回饋角色說明，明示每包內建 reviewer 及整案 integration；322 通過／2 權限跳過。第一次診斷雖入庫但建立冗餘核對包，第二次 93.554 秒形成單一產出包，仍有檢查文字矛盾。後者以原卷宗、剩餘 507.460 秒接續，worker 產物七欄錯誤，reviewer 未回覆便取消，未整合。三次共 489,969 SDK 累計 tokens（含快取），沒有追加原案預算。下一步從保存紀錄定位 worker 角色混淆與事實抽取，不追加通用提醒或同版整案抽樣；不合併／推送為已驗收。見[最新復盤與全部證據](../../evaluation/2026-09-11-initial-planning-report.md)。以下為歷史決策。

**9/11 當前決策：** 終止壓縮及缺檔回饋已修復，320 通過／2 權限跳過、封裝 36 檔。匯入正確成果診斷 291.427 秒完成核對／整合；接著正常流程 309.457 秒止於 planner 的 16 回合，34 次工具成功卻未交辦，未產出。兩次新增成本 488,074 SDK 累計 tokens（含快取）。超額後未再壓縮，不能因此宣稱規劃品質已改善。下一步核對初始規劃與回饋／異議的交辦資訊是否混雜、材料索引是否真正到位，再做有界初始規劃診斷；不加預算、不原樣跑整案、不合併／推送為已驗收。完整證據與待驗證假說見[最新復盤](../../evaluation/2026-09-11-review-boundary-report.md#正常完整流程失敗位於交辦之前)。下方為歷史決策。

**9/11 材料取得接續：** 已補 reviewer／integrator 的本 session 材料取得及版本門檻，314 通過／2 權限跳過。額外匯入既有正確成果的模型診斷 170.202 秒用完 16 回合，已讀必需材料但未提交結果。下一步先重現清單／引號被當成檔名的工具介面失誤，再判斷核對收斂方式；不重做成果、不加預算、不原樣重跑。完整證據與適用限制見[本日報告更新](../../evaluation/2026-09-11-review-boundary-report.md#後續更新材料取得門檻與核對成本)。

**9/11 最新：** b28dc6a 正常整案產出全部正確成果，454.159 秒在 reviewer 格式交接中止。已修同 session 格式拒收與 custom transport 邊界，311 通過／2 權限跳過；尚未再跑模型。更重要的是核對者零讀取且說明虛構來源值，下一步先處理核對結論的實際證據取得關係；另定位已提交結果後壓縮，不原樣重跑、不重做正確成果。詳見[核對邊界復盤](../../evaluation/2026-09-11-review-boundary-report.md)。下方「最新」均為歷史紀錄。

**最新（局部返修）：** 交辦引用錯誤回饋已修正；正常流程規劃約 46 秒，但返修重寫產物時漏掉原正確內容，601.698 秒取消。已補 worker 專用 case_edit、版本與唯一匹配檢查、SDK／trace／評估器連動；從原錯誤成果還原的局部模型診斷 40.206 秒成功，保留完整正確內容。Unicode 邊界由獨立審閱提出並以反例補修，完整 307 通過／2 權限跳過。未完成的是新工具版本的完整整合驗收，不把額外診斷當原案通過；README／狀態／英文／操作參照已同步。詳見[本輪復盤](../../evaluation/2026-09-10-localized-repair-report.md)。下方為歷史決策。

**最新補充（索引交辦）：** 四次單獨執行診斷保存成功與失敗，確認模型可完成原完整成果，但不是等預算因果比較。已實作 pi 來源預設索引，保留顯式 inline／可攜核心預設及來源版本；304 項工程測試通過、2 項權限跳過。固定正常流程已產出全部欄位正確的成果，但 planner 約 315 秒、worker 約 221 秒後，reviewer 尚未完成便達原 600 秒上限；取消收尾共 644.033 秒，未進整合。因此第 3 項整案驗收仍未通過，不勾選為完成。README、操作文件、英文與集中狀態已同步，36 檔封裝預覽通過；原案的必要材料分層方向不變，不新增角色或重建環境。接續聚焦精簡規劃、格式回饋與正確成果驗收，不原樣抽樣到成功、不將新診斷成本藏入舊預算。詳見[本輪證據與判讀](../../evaluation/2026-09-10-worker-focus-report.md)。以下保留前次決策，不冒充最新結果。

**目前決策：** 已完成角色指引一致化、單檔精確搜尋及相關工程驗證；普通旅程 332.123 秒未產出。後續由保存 payload 定位輸出額度降至 1 token，離線重現 SDK 壓縮觸發與切點估算失配；已調整有限 context 的近期保留量及截斷錯誤分類。完整回歸 302 通過／2 Windows 權限跳過。新版本正常旅程 601.856 秒仍未產出；已確認壓縮耗時 100.361 秒且輸出額度由 1 恢復到 4096，但整案到達原時間上限。模型設定、來源與上限未改。壓縮修復與任務品質分開裁定，保留工作分支，不宣稱完成整體目標、不原樣重跑。原先「異議通過才測正常旅程」的順序已明確被取代，因測試在注入否決之前分流，不能再用後段驗證阻擋前段失效定位。詳見[結果與根因](../../evaluation/2026-09-10-search-and-role-report.md)。以下保留決策演進，不作目前待辦。

9/10 受控異議診斷已執行一次，未通過；詳見[失敗證據及介面修正裁定](../../evaluation/2026-09-10-controlled-dispute-report.md)。工程回歸更新為 281 通過／1 跳過。依本計畫不啟動正常完整旅程、不原樣抽樣：下一個動作改為對齊 planner／worker 等角色的系統與工具指引，先建立 SDK 接線反例，再安排單一變因對照；不放寬驗收或預算。下段是開始執行本計畫時的先前進度。

這次調整的是執行順序、失敗隔離及證據判讀，不增加產品流程。原案的獨立 context、自查修復、即時回饋新工作、原授權內補包及只等待受影響工作，仍由既有需求追蹤維護；不因聚焦異議而刪除或重新研究。

9/10 接續：第 1 項證據保存完成，針對性測試 49 通過／1 跳過、完整 kit 278 通過／1 跳過；封裝預覽成功，README／狀態頁與英文的最新進度已同步。結果與目標關聯見[報告](../../evaluation/2026-09-10-dispute-review-report.md)。未執行新的模型診斷，未完成全部操作文件及最終交付；下一個核心工作仍是確認第 2 項邊界後執行第 3 項。預設在本任務內接續，不再要求使用者選擇執行方式。
