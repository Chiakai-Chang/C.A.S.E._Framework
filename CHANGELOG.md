# 版本紀錄

本頁記錄使用者需要知道的變更；實驗完整脈絡由 [MAP](MAP.md) 導航。預覽版不代表已證明普遍品質或成本優勢。

## 2026-09-12：預覽版接續、交辦與核對修正

Git 交付批次；套件仍為 2.0.0-preview.1、private，未發布 npm，未代選公開授權。

- 過期卷宗在呼叫模型前停止；跨執行累計預算，保留已完成成果。
- 符合安全條件的唯讀核對／整合連線失敗，可在原預算內換 session 一次；不重播執行者寫入或檢查命令，保留失敗與成本。
- 原生 pi provider 錯誤不再誤走 JSON 格式補問；收到 HTTP 回應後的串流失敗不在自動恢復範圍。
- 整理兩份首頁與狀態導航；完整多來源本機任務通過，仍保留先前語意誤驗反例。[交付驗收](docs/evaluation/2026-09-12-push-acceptance-report.md)。

- pi 核對／整合的引用改由模型選擇本 session 讀取回條 ID，程式拒絕未知／過期引用並解析實際來源，保存原始提交；不把引用存在當成語意正確。共用核心及既有卷宗不遷移。[驗證、成本與相容性](docs/evaluation/2026-09-11-receipt-review-report.md)。

- pi 初始規劃的系統與結果工具說明聚焦交辦，不預載後續異議處理責任；回饋路徑、權限、驗收與預算不變，模型效果另列。
- pi 執行者初次及返修交辦改為直接執行的分段說明；保留共識、回饋、驗收與材料，核心 JSON 不變，呈現後檢查原字數上限。單獨模型診斷與完整流程證據分開。

- pi session 終止時同步停用後續壓縮、取消已啟動壓縮，再停止生成；正常工作期間保留壓縮。case_read 缺檔錯誤提供單檔參數回饋，不自動改讀其他路徑。

- pi 內建核對／整合在回報通過前檢查本 session 是否實際讀取成果與必要來源，並核對目前版本；可在原 session 補讀。這是材料取得門檻，不是語意正確保證。

- pi 核對回覆補上角色格式驗證；工具與最後文字可在原 session 修正包錯層／缺欄位，自訂 session 回傳也會拒收。不把格式正確當成事實已核對。

- pi worker 新增 case_edit，以來源版本及唯一文字匹配執行局部修改，避免必須重新生成整份成果；沿用既有寫入範圍，記錄前後版本。

- 計畫引用錯誤回傳工作包、欄位／檢查及合法契約編號，保留原拒絕規則；pi 指引明示 delivery 僅位於 inputs 項目內。

- pi 執行者的未指定材料傳送方式改為版本化索引；明確 inline 仍附全文，CLI／核心原預設不變。保留目標、限制、驗收、來源有效性與必讀義務。
- pi 依模型 context 大小調整 SDK 壓縮的近期保留量，避免 32K 環境沿用過大的預設值；恢復後仍截斷時回報 `MODEL_OUTPUT_TRUNCATED`，不誤當 JSON 格式錯誤追加重試。模型效果與工程修正分開驗證。
- pi 新增有界 `case_search`，可按字面定位單檔中的精確行與來源版本；不增加寫入或任意命令權限。
- 規劃／執行／核對的系統與結果工具指引按角色對齊；拒收混合規劃回覆。
- 有證據的審查異議可在不改寫成果下請求一次新 session 核對，保留原驗收、累計預算與中斷歷史。模型效果不保證，失敗及新觀察見 READINESS。

- 接續改善交辦資訊：pi 規劃可查不含正文的材料概況；工作包明示已附全文與待讀參照，保留必要重讀。這是待實測的工作方法調整，不保證模型遵從或品質提升。

- pi 讀取結果加入模型可見的來源版本、實際行範圍、下一頁及空檔／超尾說明；EOF 不再容易被誤認為已取得整份檔案。
- 新增有容量上限的事件診斷，缺漏明示；不宣稱模型已理解材料，也不宣稱既有紀錄已自動去敏。
- 紀錄保存失敗時停止派工，保留原操作錯誤、保存錯誤及可取得的記憶體證據，不自動重播副作用。
- 沒有新增必填設定或改變 init／卷宗格式；[驗證與模型結果](docs/evaluation/2026-09-06-read-receipt-report.md)與先前發布分開記錄。

## 2.0.0-preview.1

- 新增版本化工作契約與工作包；專案共識、整體計畫、當前材料分層保存。
- pi 原生套件自動建立規劃、執行、獨立核對與整合 session；可攜技能及 v1 輕量操作保留。
- 執行中可立即回報新工作，規劃者在既有授權內去重、補前置與安排接續；只有受影響工作等待。
- 提交前查實際產物與核准檢查，拒收後允許原 session 修復；接受後由程式交回外層，不依賴模型自行停止。
- 保存來源／產物版本、失敗、處置與用量；必要材料及發現證據按需讀取，不將全部歷史塞入每個 context。
- 首頁、套件首頁、操作、安裝及英文指南按實際採用旅程整理；新手不需先理解舊研究代號。
- 修正直接載入工作包材料可能繞過設定目錄保護的缺口；核心與 pi 工具共用規則，既有不安全工作包也會拒讀。

### 升級注意

技能更新不會自動遷移資料。v1 使用者先停止舊寫者，再依 [v2 升級指南](workflow-kit/docs/V2.md#v1-升級) 明示遷移，保留回傳的備份位置。啟用新待辦後卷宗 state 為 `case-workflow/2.1`，共同使用該卷宗的核心都需更新；目錄 manifest 仍為 v2。

### 已知限制

只有 pi 提供本套件的原生自動 session 整合；Codex／Claude Code／Antigravity 使用可攜技能與核心。模型效果、平台與操作驗證範圍見 [READINESS](workflow-kit/docs/READINESS.md)。沒有作業系統沙箱、零失敗保證或 npm registry 發布；公開授權仍未選定。

## 先前 v1

提供任務記錄、檢查點、交接、驗收證據及重新開啟；[v1 操作實例](workflow-kit/docs/WORKFLOW.md)持續保留。歷史驗收與實驗結果不因 v2 新增功能而改寫。
